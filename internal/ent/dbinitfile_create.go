// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"stoke/internal/ent/dbinitfile"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// DBInitFileCreate is the builder for creating a DBInitFile entity.
type DBInitFileCreate struct {
	config
	mutation *DBInitFileMutation
	hooks    []Hook
}

// SetFilename sets the "filename" field.
func (difc *DBInitFileCreate) SetFilename(s string) *DBInitFileCreate {
	difc.mutation.SetFilename(s)
	return difc
}

// SetMd5 sets the "md5" field.
func (difc *DBInitFileCreate) SetMd5(s string) *DBInitFileCreate {
	difc.mutation.SetMd5(s)
	return difc
}

// Mutation returns the DBInitFileMutation object of the builder.
func (difc *DBInitFileCreate) Mutation() *DBInitFileMutation {
	return difc.mutation
}

// Save creates the DBInitFile in the database.
func (difc *DBInitFileCreate) Save(ctx context.Context) (*DBInitFile, error) {
	return withHooks(ctx, difc.sqlSave, difc.mutation, difc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (difc *DBInitFileCreate) SaveX(ctx context.Context) *DBInitFile {
	v, err := difc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (difc *DBInitFileCreate) Exec(ctx context.Context) error {
	_, err := difc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (difc *DBInitFileCreate) ExecX(ctx context.Context) {
	if err := difc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (difc *DBInitFileCreate) check() error {
	if _, ok := difc.mutation.Filename(); !ok {
		return &ValidationError{Name: "filename", err: errors.New(`ent: missing required field "DBInitFile.filename"`)}
	}
	if _, ok := difc.mutation.Md5(); !ok {
		return &ValidationError{Name: "md5", err: errors.New(`ent: missing required field "DBInitFile.md5"`)}
	}
	return nil
}

func (difc *DBInitFileCreate) sqlSave(ctx context.Context) (*DBInitFile, error) {
	if err := difc.check(); err != nil {
		return nil, err
	}
	_node, _spec := difc.createSpec()
	if err := sqlgraph.CreateNode(ctx, difc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	difc.mutation.id = &_node.ID
	difc.mutation.done = true
	return _node, nil
}

func (difc *DBInitFileCreate) createSpec() (*DBInitFile, *sqlgraph.CreateSpec) {
	var (
		_node = &DBInitFile{config: difc.config}
		_spec = sqlgraph.NewCreateSpec(dbinitfile.Table, sqlgraph.NewFieldSpec(dbinitfile.FieldID, field.TypeInt))
	)
	if value, ok := difc.mutation.Filename(); ok {
		_spec.SetField(dbinitfile.FieldFilename, field.TypeString, value)
		_node.Filename = value
	}
	if value, ok := difc.mutation.Md5(); ok {
		_spec.SetField(dbinitfile.FieldMd5, field.TypeString, value)
		_node.Md5 = value
	}
	return _node, _spec
}

// DBInitFileCreateBulk is the builder for creating many DBInitFile entities in bulk.
type DBInitFileCreateBulk struct {
	config
	err      error
	builders []*DBInitFileCreate
}

// Save creates the DBInitFile entities in the database.
func (difcb *DBInitFileCreateBulk) Save(ctx context.Context) ([]*DBInitFile, error) {
	if difcb.err != nil {
		return nil, difcb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(difcb.builders))
	nodes := make([]*DBInitFile, len(difcb.builders))
	mutators := make([]Mutator, len(difcb.builders))
	for i := range difcb.builders {
		func(i int, root context.Context) {
			builder := difcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*DBInitFileMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, difcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, difcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, difcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (difcb *DBInitFileCreateBulk) SaveX(ctx context.Context) []*DBInitFile {
	v, err := difcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (difcb *DBInitFileCreateBulk) Exec(ctx context.Context) error {
	_, err := difcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (difcb *DBInitFileCreateBulk) ExecX(ctx context.Context) {
	if err := difcb.Exec(ctx); err != nil {
		panic(err)
	}
}
