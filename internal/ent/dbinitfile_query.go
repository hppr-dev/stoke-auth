// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"stoke/internal/ent/dbinitfile"
	"stoke/internal/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// DBInitFileQuery is the builder for querying DBInitFile entities.
type DBInitFileQuery struct {
	config
	ctx        *QueryContext
	order      []dbinitfile.OrderOption
	inters     []Interceptor
	predicates []predicate.DBInitFile
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DBInitFileQuery builder.
func (difq *DBInitFileQuery) Where(ps ...predicate.DBInitFile) *DBInitFileQuery {
	difq.predicates = append(difq.predicates, ps...)
	return difq
}

// Limit the number of records to be returned by this query.
func (difq *DBInitFileQuery) Limit(limit int) *DBInitFileQuery {
	difq.ctx.Limit = &limit
	return difq
}

// Offset to start from.
func (difq *DBInitFileQuery) Offset(offset int) *DBInitFileQuery {
	difq.ctx.Offset = &offset
	return difq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (difq *DBInitFileQuery) Unique(unique bool) *DBInitFileQuery {
	difq.ctx.Unique = &unique
	return difq
}

// Order specifies how the records should be ordered.
func (difq *DBInitFileQuery) Order(o ...dbinitfile.OrderOption) *DBInitFileQuery {
	difq.order = append(difq.order, o...)
	return difq
}

// First returns the first DBInitFile entity from the query.
// Returns a *NotFoundError when no DBInitFile was found.
func (difq *DBInitFileQuery) First(ctx context.Context) (*DBInitFile, error) {
	nodes, err := difq.Limit(1).All(setContextOp(ctx, difq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{dbinitfile.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (difq *DBInitFileQuery) FirstX(ctx context.Context) *DBInitFile {
	node, err := difq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DBInitFile ID from the query.
// Returns a *NotFoundError when no DBInitFile ID was found.
func (difq *DBInitFileQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = difq.Limit(1).IDs(setContextOp(ctx, difq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{dbinitfile.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (difq *DBInitFileQuery) FirstIDX(ctx context.Context) int {
	id, err := difq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DBInitFile entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DBInitFile entity is found.
// Returns a *NotFoundError when no DBInitFile entities are found.
func (difq *DBInitFileQuery) Only(ctx context.Context) (*DBInitFile, error) {
	nodes, err := difq.Limit(2).All(setContextOp(ctx, difq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{dbinitfile.Label}
	default:
		return nil, &NotSingularError{dbinitfile.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (difq *DBInitFileQuery) OnlyX(ctx context.Context) *DBInitFile {
	node, err := difq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DBInitFile ID in the query.
// Returns a *NotSingularError when more than one DBInitFile ID is found.
// Returns a *NotFoundError when no entities are found.
func (difq *DBInitFileQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = difq.Limit(2).IDs(setContextOp(ctx, difq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{dbinitfile.Label}
	default:
		err = &NotSingularError{dbinitfile.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (difq *DBInitFileQuery) OnlyIDX(ctx context.Context) int {
	id, err := difq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DBInitFiles.
func (difq *DBInitFileQuery) All(ctx context.Context) ([]*DBInitFile, error) {
	ctx = setContextOp(ctx, difq.ctx, "All")
	if err := difq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*DBInitFile, *DBInitFileQuery]()
	return withInterceptors[[]*DBInitFile](ctx, difq, qr, difq.inters)
}

// AllX is like All, but panics if an error occurs.
func (difq *DBInitFileQuery) AllX(ctx context.Context) []*DBInitFile {
	nodes, err := difq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DBInitFile IDs.
func (difq *DBInitFileQuery) IDs(ctx context.Context) (ids []int, err error) {
	if difq.ctx.Unique == nil && difq.path != nil {
		difq.Unique(true)
	}
	ctx = setContextOp(ctx, difq.ctx, "IDs")
	if err = difq.Select(dbinitfile.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (difq *DBInitFileQuery) IDsX(ctx context.Context) []int {
	ids, err := difq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (difq *DBInitFileQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, difq.ctx, "Count")
	if err := difq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, difq, querierCount[*DBInitFileQuery](), difq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (difq *DBInitFileQuery) CountX(ctx context.Context) int {
	count, err := difq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (difq *DBInitFileQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, difq.ctx, "Exist")
	switch _, err := difq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (difq *DBInitFileQuery) ExistX(ctx context.Context) bool {
	exist, err := difq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DBInitFileQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (difq *DBInitFileQuery) Clone() *DBInitFileQuery {
	if difq == nil {
		return nil
	}
	return &DBInitFileQuery{
		config:     difq.config,
		ctx:        difq.ctx.Clone(),
		order:      append([]dbinitfile.OrderOption{}, difq.order...),
		inters:     append([]Interceptor{}, difq.inters...),
		predicates: append([]predicate.DBInitFile{}, difq.predicates...),
		// clone intermediate query.
		sql:  difq.sql.Clone(),
		path: difq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Filename string `json:"filename,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.DBInitFile.Query().
//		GroupBy(dbinitfile.FieldFilename).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (difq *DBInitFileQuery) GroupBy(field string, fields ...string) *DBInitFileGroupBy {
	difq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &DBInitFileGroupBy{build: difq}
	grbuild.flds = &difq.ctx.Fields
	grbuild.label = dbinitfile.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Filename string `json:"filename,omitempty"`
//	}
//
//	client.DBInitFile.Query().
//		Select(dbinitfile.FieldFilename).
//		Scan(ctx, &v)
func (difq *DBInitFileQuery) Select(fields ...string) *DBInitFileSelect {
	difq.ctx.Fields = append(difq.ctx.Fields, fields...)
	sbuild := &DBInitFileSelect{DBInitFileQuery: difq}
	sbuild.label = dbinitfile.Label
	sbuild.flds, sbuild.scan = &difq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a DBInitFileSelect configured with the given aggregations.
func (difq *DBInitFileQuery) Aggregate(fns ...AggregateFunc) *DBInitFileSelect {
	return difq.Select().Aggregate(fns...)
}

func (difq *DBInitFileQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range difq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, difq); err != nil {
				return err
			}
		}
	}
	for _, f := range difq.ctx.Fields {
		if !dbinitfile.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if difq.path != nil {
		prev, err := difq.path(ctx)
		if err != nil {
			return err
		}
		difq.sql = prev
	}
	return nil
}

func (difq *DBInitFileQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*DBInitFile, error) {
	var (
		nodes = []*DBInitFile{}
		_spec = difq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*DBInitFile).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &DBInitFile{config: difq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, difq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (difq *DBInitFileQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := difq.querySpec()
	_spec.Node.Columns = difq.ctx.Fields
	if len(difq.ctx.Fields) > 0 {
		_spec.Unique = difq.ctx.Unique != nil && *difq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, difq.driver, _spec)
}

func (difq *DBInitFileQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(dbinitfile.Table, dbinitfile.Columns, sqlgraph.NewFieldSpec(dbinitfile.FieldID, field.TypeInt))
	_spec.From = difq.sql
	if unique := difq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if difq.path != nil {
		_spec.Unique = true
	}
	if fields := difq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, dbinitfile.FieldID)
		for i := range fields {
			if fields[i] != dbinitfile.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := difq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := difq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := difq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := difq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (difq *DBInitFileQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(difq.driver.Dialect())
	t1 := builder.Table(dbinitfile.Table)
	columns := difq.ctx.Fields
	if len(columns) == 0 {
		columns = dbinitfile.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if difq.sql != nil {
		selector = difq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if difq.ctx.Unique != nil && *difq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range difq.predicates {
		p(selector)
	}
	for _, p := range difq.order {
		p(selector)
	}
	if offset := difq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := difq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// DBInitFileGroupBy is the group-by builder for DBInitFile entities.
type DBInitFileGroupBy struct {
	selector
	build *DBInitFileQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (difgb *DBInitFileGroupBy) Aggregate(fns ...AggregateFunc) *DBInitFileGroupBy {
	difgb.fns = append(difgb.fns, fns...)
	return difgb
}

// Scan applies the selector query and scans the result into the given value.
func (difgb *DBInitFileGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, difgb.build.ctx, "GroupBy")
	if err := difgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DBInitFileQuery, *DBInitFileGroupBy](ctx, difgb.build, difgb, difgb.build.inters, v)
}

func (difgb *DBInitFileGroupBy) sqlScan(ctx context.Context, root *DBInitFileQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(difgb.fns))
	for _, fn := range difgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*difgb.flds)+len(difgb.fns))
		for _, f := range *difgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*difgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := difgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// DBInitFileSelect is the builder for selecting fields of DBInitFile entities.
type DBInitFileSelect struct {
	*DBInitFileQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (difs *DBInitFileSelect) Aggregate(fns ...AggregateFunc) *DBInitFileSelect {
	difs.fns = append(difs.fns, fns...)
	return difs
}

// Scan applies the selector query and scans the result into the given value.
func (difs *DBInitFileSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, difs.ctx, "Select")
	if err := difs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DBInitFileQuery, *DBInitFileSelect](ctx, difs.DBInitFileQuery, difs, difs.inters, v)
}

func (difs *DBInitFileSelect) sqlScan(ctx context.Context, root *DBInitFileQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(difs.fns))
	for _, fn := range difs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*difs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := difs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
