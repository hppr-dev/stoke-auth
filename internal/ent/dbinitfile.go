// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"stoke/internal/ent/dbinitfile"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// DBInitFile is the model entity for the DBInitFile schema.
type DBInitFile struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Filename holds the value of the "filename" field.
	Filename string `json:"filename,omitempty"`
	// Md5 holds the value of the "md5" field.
	Md5          string `json:"md5,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DBInitFile) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case dbinitfile.FieldID:
			values[i] = new(sql.NullInt64)
		case dbinitfile.FieldFilename, dbinitfile.FieldMd5:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DBInitFile fields.
func (dif *DBInitFile) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case dbinitfile.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			dif.ID = int(value.Int64)
		case dbinitfile.FieldFilename:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field filename", values[i])
			} else if value.Valid {
				dif.Filename = value.String
			}
		case dbinitfile.FieldMd5:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field md5", values[i])
			} else if value.Valid {
				dif.Md5 = value.String
			}
		default:
			dif.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DBInitFile.
// This includes values selected through modifiers, order, etc.
func (dif *DBInitFile) Value(name string) (ent.Value, error) {
	return dif.selectValues.Get(name)
}

// Update returns a builder for updating this DBInitFile.
// Note that you need to call DBInitFile.Unwrap() before calling this method if this DBInitFile
// was returned from a transaction, and the transaction was committed or rolled back.
func (dif *DBInitFile) Update() *DBInitFileUpdateOne {
	return NewDBInitFileClient(dif.config).UpdateOne(dif)
}

// Unwrap unwraps the DBInitFile entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (dif *DBInitFile) Unwrap() *DBInitFile {
	_tx, ok := dif.config.driver.(*txDriver)
	if !ok {
		panic("ent: DBInitFile is not a transactional entity")
	}
	dif.config.driver = _tx.drv
	return dif
}

// String implements the fmt.Stringer.
func (dif *DBInitFile) String() string {
	var builder strings.Builder
	builder.WriteString("DBInitFile(")
	builder.WriteString(fmt.Sprintf("id=%v, ", dif.ID))
	builder.WriteString("filename=")
	builder.WriteString(dif.Filename)
	builder.WriteString(", ")
	builder.WriteString("md5=")
	builder.WriteString(dif.Md5)
	builder.WriteByte(')')
	return builder.String()
}

// DBInitFiles is a parsable slice of DBInitFile.
type DBInitFiles []*DBInitFile
