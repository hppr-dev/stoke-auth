// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"stoke/internal/ent/predicate"
	"stoke/internal/ent/privatekey"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PrivateKeyQuery is the builder for querying PrivateKey entities.
type PrivateKeyQuery struct {
	config
	ctx        *QueryContext
	order      []privatekey.OrderOption
	inters     []Interceptor
	predicates []predicate.PrivateKey
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PrivateKeyQuery builder.
func (pkq *PrivateKeyQuery) Where(ps ...predicate.PrivateKey) *PrivateKeyQuery {
	pkq.predicates = append(pkq.predicates, ps...)
	return pkq
}

// Limit the number of records to be returned by this query.
func (pkq *PrivateKeyQuery) Limit(limit int) *PrivateKeyQuery {
	pkq.ctx.Limit = &limit
	return pkq
}

// Offset to start from.
func (pkq *PrivateKeyQuery) Offset(offset int) *PrivateKeyQuery {
	pkq.ctx.Offset = &offset
	return pkq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pkq *PrivateKeyQuery) Unique(unique bool) *PrivateKeyQuery {
	pkq.ctx.Unique = &unique
	return pkq
}

// Order specifies how the records should be ordered.
func (pkq *PrivateKeyQuery) Order(o ...privatekey.OrderOption) *PrivateKeyQuery {
	pkq.order = append(pkq.order, o...)
	return pkq
}

// First returns the first PrivateKey entity from the query.
// Returns a *NotFoundError when no PrivateKey was found.
func (pkq *PrivateKeyQuery) First(ctx context.Context) (*PrivateKey, error) {
	nodes, err := pkq.Limit(1).All(setContextOp(ctx, pkq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{privatekey.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pkq *PrivateKeyQuery) FirstX(ctx context.Context) *PrivateKey {
	node, err := pkq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PrivateKey ID from the query.
// Returns a *NotFoundError when no PrivateKey ID was found.
func (pkq *PrivateKeyQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pkq.Limit(1).IDs(setContextOp(ctx, pkq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{privatekey.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pkq *PrivateKeyQuery) FirstIDX(ctx context.Context) int {
	id, err := pkq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PrivateKey entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PrivateKey entity is found.
// Returns a *NotFoundError when no PrivateKey entities are found.
func (pkq *PrivateKeyQuery) Only(ctx context.Context) (*PrivateKey, error) {
	nodes, err := pkq.Limit(2).All(setContextOp(ctx, pkq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{privatekey.Label}
	default:
		return nil, &NotSingularError{privatekey.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pkq *PrivateKeyQuery) OnlyX(ctx context.Context) *PrivateKey {
	node, err := pkq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PrivateKey ID in the query.
// Returns a *NotSingularError when more than one PrivateKey ID is found.
// Returns a *NotFoundError when no entities are found.
func (pkq *PrivateKeyQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pkq.Limit(2).IDs(setContextOp(ctx, pkq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{privatekey.Label}
	default:
		err = &NotSingularError{privatekey.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pkq *PrivateKeyQuery) OnlyIDX(ctx context.Context) int {
	id, err := pkq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PrivateKeys.
func (pkq *PrivateKeyQuery) All(ctx context.Context) ([]*PrivateKey, error) {
	ctx = setContextOp(ctx, pkq.ctx, "All")
	if err := pkq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PrivateKey, *PrivateKeyQuery]()
	return withInterceptors[[]*PrivateKey](ctx, pkq, qr, pkq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pkq *PrivateKeyQuery) AllX(ctx context.Context) []*PrivateKey {
	nodes, err := pkq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PrivateKey IDs.
func (pkq *PrivateKeyQuery) IDs(ctx context.Context) (ids []int, err error) {
	if pkq.ctx.Unique == nil && pkq.path != nil {
		pkq.Unique(true)
	}
	ctx = setContextOp(ctx, pkq.ctx, "IDs")
	if err = pkq.Select(privatekey.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pkq *PrivateKeyQuery) IDsX(ctx context.Context) []int {
	ids, err := pkq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pkq *PrivateKeyQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pkq.ctx, "Count")
	if err := pkq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pkq, querierCount[*PrivateKeyQuery](), pkq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pkq *PrivateKeyQuery) CountX(ctx context.Context) int {
	count, err := pkq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pkq *PrivateKeyQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pkq.ctx, "Exist")
	switch _, err := pkq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pkq *PrivateKeyQuery) ExistX(ctx context.Context) bool {
	exist, err := pkq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PrivateKeyQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pkq *PrivateKeyQuery) Clone() *PrivateKeyQuery {
	if pkq == nil {
		return nil
	}
	return &PrivateKeyQuery{
		config:     pkq.config,
		ctx:        pkq.ctx.Clone(),
		order:      append([]privatekey.OrderOption{}, pkq.order...),
		inters:     append([]Interceptor{}, pkq.inters...),
		predicates: append([]predicate.PrivateKey{}, pkq.predicates...),
		// clone intermediate query.
		sql:  pkq.sql.Clone(),
		path: pkq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Text string `json:"text,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PrivateKey.Query().
//		GroupBy(privatekey.FieldText).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pkq *PrivateKeyQuery) GroupBy(field string, fields ...string) *PrivateKeyGroupBy {
	pkq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PrivateKeyGroupBy{build: pkq}
	grbuild.flds = &pkq.ctx.Fields
	grbuild.label = privatekey.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Text string `json:"text,omitempty"`
//	}
//
//	client.PrivateKey.Query().
//		Select(privatekey.FieldText).
//		Scan(ctx, &v)
func (pkq *PrivateKeyQuery) Select(fields ...string) *PrivateKeySelect {
	pkq.ctx.Fields = append(pkq.ctx.Fields, fields...)
	sbuild := &PrivateKeySelect{PrivateKeyQuery: pkq}
	sbuild.label = privatekey.Label
	sbuild.flds, sbuild.scan = &pkq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PrivateKeySelect configured with the given aggregations.
func (pkq *PrivateKeyQuery) Aggregate(fns ...AggregateFunc) *PrivateKeySelect {
	return pkq.Select().Aggregate(fns...)
}

func (pkq *PrivateKeyQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pkq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pkq); err != nil {
				return err
			}
		}
	}
	for _, f := range pkq.ctx.Fields {
		if !privatekey.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pkq.path != nil {
		prev, err := pkq.path(ctx)
		if err != nil {
			return err
		}
		pkq.sql = prev
	}
	return nil
}

func (pkq *PrivateKeyQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PrivateKey, error) {
	var (
		nodes = []*PrivateKey{}
		_spec = pkq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PrivateKey).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PrivateKey{config: pkq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pkq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (pkq *PrivateKeyQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pkq.querySpec()
	_spec.Node.Columns = pkq.ctx.Fields
	if len(pkq.ctx.Fields) > 0 {
		_spec.Unique = pkq.ctx.Unique != nil && *pkq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pkq.driver, _spec)
}

func (pkq *PrivateKeyQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(privatekey.Table, privatekey.Columns, sqlgraph.NewFieldSpec(privatekey.FieldID, field.TypeInt))
	_spec.From = pkq.sql
	if unique := pkq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pkq.path != nil {
		_spec.Unique = true
	}
	if fields := pkq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, privatekey.FieldID)
		for i := range fields {
			if fields[i] != privatekey.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := pkq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pkq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pkq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pkq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pkq *PrivateKeyQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pkq.driver.Dialect())
	t1 := builder.Table(privatekey.Table)
	columns := pkq.ctx.Fields
	if len(columns) == 0 {
		columns = privatekey.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pkq.sql != nil {
		selector = pkq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pkq.ctx.Unique != nil && *pkq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range pkq.predicates {
		p(selector)
	}
	for _, p := range pkq.order {
		p(selector)
	}
	if offset := pkq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pkq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PrivateKeyGroupBy is the group-by builder for PrivateKey entities.
type PrivateKeyGroupBy struct {
	selector
	build *PrivateKeyQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pkgb *PrivateKeyGroupBy) Aggregate(fns ...AggregateFunc) *PrivateKeyGroupBy {
	pkgb.fns = append(pkgb.fns, fns...)
	return pkgb
}

// Scan applies the selector query and scans the result into the given value.
func (pkgb *PrivateKeyGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pkgb.build.ctx, "GroupBy")
	if err := pkgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PrivateKeyQuery, *PrivateKeyGroupBy](ctx, pkgb.build, pkgb, pkgb.build.inters, v)
}

func (pkgb *PrivateKeyGroupBy) sqlScan(ctx context.Context, root *PrivateKeyQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pkgb.fns))
	for _, fn := range pkgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pkgb.flds)+len(pkgb.fns))
		for _, f := range *pkgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pkgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pkgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PrivateKeySelect is the builder for selecting fields of PrivateKey entities.
type PrivateKeySelect struct {
	*PrivateKeyQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pks *PrivateKeySelect) Aggregate(fns ...AggregateFunc) *PrivateKeySelect {
	pks.fns = append(pks.fns, fns...)
	return pks
}

// Scan applies the selector query and scans the result into the given value.
func (pks *PrivateKeySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pks.ctx, "Select")
	if err := pks.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PrivateKeyQuery, *PrivateKeySelect](ctx, pks.PrivateKeyQuery, pks, pks.inters, v)
}

func (pks *PrivateKeySelect) sqlScan(ctx context.Context, root *PrivateKeyQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pks.fns))
	for _, fn := range pks.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pks.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pks.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
