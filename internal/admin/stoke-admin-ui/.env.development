# Backend origin for API and metrics when running the UI dev server (different port).
# Uncomment and set so API calls hit the Go server (e.g. http://localhost:8080).
# VITE_API_URL="http://localhost:8080"
